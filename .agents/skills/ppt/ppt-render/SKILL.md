---
name: ppt-render
description: Use when building `02_deck.json` into deck.pptx — python-pptx 렌더, 미리보기 PNG·PDF 산출, 빌드 리포트. 빌드까지만 소유하며 합격 판정은 하지 않는다. 검증은 ppt-verify 가 맡는다
---

# ppt-render

Codex 어댑터다. 정본 `.claude/skills/ppt/ppt-render/SKILL.md` 를 읽고 실행한다. `scripts/`·`contract/`·
`case/`·`references/` 도 정본 디렉토리에 있다. 생성물이므로 직접 고치지 않는다.
