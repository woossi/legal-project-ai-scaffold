---
name: ppt-source
description: Use when collecting 발표 덱의 근거 자산 into `01_source.json` — 원천 경로·소유팀·sha256·기준시점 등록, 수치를 실행 시점에 직접 세어 fact 로 발급, 분모와 한계 명시. 남의 요약표를 복제하지 않는다
---

# ppt-source

Codex 어댑터다. 정본 `.claude/skills/ppt/ppt-source/SKILL.md` 를 읽고 실행한다. `scripts/`·`contract/`·
`case/`·`references/` 도 정본 디렉토리에 있다. 생성물이므로 직접 고치지 않는다.
