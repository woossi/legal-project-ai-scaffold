# 판정 규칙과 검증 게이트

`SKILL.md` 의 실행 절이 이 문서를 정본으로 가리킨다.

## 판정 규칙

- 법령명 어휘는 2단으로 만든다. 1단은 통째로 법령명+조문참조인 인용에서 학습하고, 2단은
  학습한 어휘로 지저분한 문자열에서 최장 일치를 찾는다
- **어휘는 인용부호로 시작하는 인용에서만 학습한다.** 법령명은 관례상 「」로 묶이고,
  묶이지 않은 인용에는 앞 문장의 서술이 붙어 들어온다
- 인용부호 없는 인용은, 1단 어휘로 스캔해 **0번이 아닌 위치에서 아는 법령이 나오면**
  버린다. 그 앞부분이 서술 잔여물이라는 뜻이다(`말하여 이때 건축법`). 아는 법령이
  없으면 새 법령이므로 학습한다(`도로법`). 어미 목록으로 거르는 방식은 `위하여`·
  `높이는`·`정해진` 을 놓쳐 서술 잔여물 8종이 법령 노드로 올라왔다
- 정의문 도입부(`…라 함은`)가 든 문자열은 법령명이 아니다
- 조례에 지자체명이 없으면 근거 문장 바로 앞에서 되살린다. 같은 문장이 약칭을 정의한
  경우(`인천시 도시계획조례(이하 도시계획조례라 한다)`)는 앞선 조례로 잇는다
- **정본 대조가 어휘 결함의 검출기다.** 서술 잔여물은 API 에서 결과가 나오지 않으므로
  미대조로 드러난다. 미대조 목록을 보고 어휘 학습을 고친다
- 조회 대상은 표기 꼬리로 가른다. `조례` → 자치법규, `지침·고시·예규·훈령·기준` →
  행정규칙 우선, 그 밖에는 법령 우선이며 실패하면 행정규칙을 시도한다
- 자치법규 검색은 지자체 가나다순으로 대량 반환한다. `display` 를 작게 두면 본청 조례를
  놓친다(`서울특별시 건축 조례` 는 123건 중 뒤쪽)
- 자치법규는 정식 행정구역명만 받는다. `서울시 건축조례` 는 0건이므로 약칭을 편다
- 중점 표기는 원본마다 다르다(`ㆍ · ‧ ․ ･ ・ ∙`). 쉼표로 적은 문서도 있다. 대조 키에서
  접고 본문 매칭에서 다시 허용한다. `case/정본대조.json` 의 키도 같은 폭으로 접힌다
- `기법`·`공법`·`수법` 으로 끝나는 것은 `법` 으로 끝나도 법령이 아니다
- 명칭 변천은 `case/정본대조.json` 이 확인한 것만 받는다. 단일 후보라는 이유로 받지 않는다

## 검증 게이트

| 번호 | 실패 조건 | 검사 정본 |
|---:|---|---|
| 1 | 입력 문서 수가 계약 모수와 다름 | `build_guideline_article_scope.py` |
| 2 | h4 조문 없는 문서가 `fallback_unsegmented_document` 없이 제외됨 | `verify_guideline_articles.py` |
| 3 | 첫 h4 전 본문이 `unassigned_document_preamble` 없이 제외됨 | 같은 검증기 |
| 4 | mention의 지구·파일·범위·줄·근거 중 하나가 없음 | `contract/outputs.json` |
| 5 | `옥외광고물_수집범위`의 네 자료 중 하나가 없음 | case + 같은 검증기 |
| 6 | 관측 명칭이나 지구번호 연도 proxy를 정본명·적용일로 승격함 | contract + 같은 검증기 |
| 7 | 같은 입력의 JSON·Markdown 재생성 결과가 다름 | `verify_statute.py` |
| 8 | 독립 직접인용 누락 또는 구조 오류가 있는데 수집을 시작함 | review 산출물 |
| 9 | 정본대조 source의 전문 문서·본문 단위가 없음 | 같은 검증기 |
| 10 | 403·429·차단문구 뒤 우회하거나 캐시 없이 요청을 반복함 | crawl report |
| 11 | 직접범위와 T4의 기원을 섞거나 T4를 재귀 확장함 | 같은 검증기 |
| 12 | 원 시행지침 corpus의 본문·줄범위·해시가 Markdown과 다름 | 같은 검증기 |
| 13 | 비조문형 전문을 번호 형식 프로파일 선언 없이 파싱함 | `contract/hang_numbering.json` |

## 비조문형 전문의 번호 형식

줄머리 번호 형식은 문서마다 다르다. 실측하면 corpus 의 비조문형 9건 중 x-y-z 3단이
지배하는 것은 3건뿐이다. 도시·군관리계획수립지침은 4단 359건, 도로안전시설 지침은
`N.` 319건과 가나다목 282건이다. **3단을 상수로 두면 대부분을 놓친다.**

절차와 게이트 9종의 정본은 `contract/hang_numbering.json` 이고 실행은
`verify_hang_numbering.py` 다. 규칙만 옮겨 적는다.

- shape 은 번호와 종결부호까지만 본다. 종결부호 뒤 본문을 넣으면
  `2-2-6. 2-2-5.에도` 가 별개 shape 으로 갈려 인벤토리가 터진다
- **미선언 shape 은 빈도가 낮아도 버리지 않는다.** 대표 줄을 원문에서 열어 역할을
  정하고 프로파일에 선언한 뒤 다시 돌린다
- 전각 하이픈(U+FF0D)·전각 마침표(U+FF0E)를 구분자 도메인에서 빼면 `１－１－１．` 이
  shape 을 못 받고 `<본문>` 으로 떨어진다. 미탐지가 본문으로 위장하는 것이 이 게이트가
  막는 실패다
- 중점(`ㆍ·‧`)은 구분자 도메인에 넣지 않는다. 본문 열거 구분자로 압도적으로 쓰인다
- shape 이 같아도 하이픈 변형이 섞일 수 있다. `1-1-1.` 과 `1–1–2.` 는 둘 다
  `N-N-N.` 이므로 shape 이 아니라 게이트 5(문자도메인 혼용)가 잡는다
- 최빈 shape 이 항이라는 보장이 없다. `N.` 은 지구단위계획수립지침에서 예시 계산
  단계이고 도로안전시설 지침에서는 지배 형식이다. 역할은 문서마다 다시 정한다
