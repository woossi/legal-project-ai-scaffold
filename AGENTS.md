# 프로젝트 목적

- 택지개발지구사업 관련 법령제도 자료 수집, 그래프화

# 팀 목록

- legal, kb, viz, ppt 팀 존재

# 규칙

## 작업 규칙
- Codex 에이전트 팀 기능 가장 우선 활용해 작업
- 프로젝트 내 에이전트 메모리는 에이전트별 MEMORY.md 단일 파일로 유지
- 재사용 가능한 규칙·절차·실측치는 메모리가 아니라 스킬에 적재
- 산출물은 `output/<팀>/` 아래에 위치시킬 것

### PPT 팀 경계

- ppt 팀은 `output/legal/`, `output/kb/`, `output/viz/`의 구조화 산출물을 입력으로 사용한다.
- ppt 팀은 원본 PDF·HWP를 다시 파싱하지 않는다.
- ppt 팀의 작업 산출물은 `output/ppt/<deck-id>/` 아래에 둔다.
- `docs/ppt/`는 채택된 발표자료 정본 보관 경로다. 새 덱은 그 디자인을 선택 프로필로만 사용한다. 사용자가 최종 반영을 지시한 경우에만 갱신한다.
- 다른 저장소의 로컬 경로는 런타임 의존 경로로 사용하지 않는다.

### 에이전트 메모리 계약

- 적용 대상은 `.claude/agent-memory/<에이전트>/MEMORY.md`이다.
- 에이전트별 메모리는 `MEMORY.md` 하나만 유지하고 topic 파일은 만들지 않는다.
- 파일 상단에는 `reviewed_at`과 `evidence_commit`을 둔다.
- 섹션은 `정본 포인터`, `현재 상태`, `미해결·인계`로 고정한다.
- `현재 상태`의 각 항목은 대상, 상태(`active|needs_recheck`), 근거, 확인일, 재검증 조건을 갖춘다.
- `미해결·인계`의 각 항목은 대상, 다음 행동, 담당, 근거를 갖춘다.
- 완료·해결 이력, 장문 절차, 반복 가능한 규칙·실측치는 메모리에 남기지 않는다. 재사용 내용은 기존 팀 스킬 정본으로 옮기고 메모리에는 포인터만 둔다.
- 메모리는 200줄 또는 25KB보다 작게 유지한다.

## 작성 규칙

- skills, agents, tasks의 md 파일은 동일한 형식 구조를 갖추도록 할 것
- 컨텍스트에 적재될 수 있는 md 파일의 문장은 명료성을 지켜서 작업할 것
- `.codex/rules`는 프로젝트의 작업에 수행에 있어 용어를 명시적으로 정의해야할 때 작성

### Skills 규칙

#### 폴더 규약

```
.claude/skills/<팀>/<스킬명>/    구현 정본
.agents/skills/<팀>/<스킬명>/    Codex 라우팅 어댑터
  SKILL.md            필수. 규칙·절차 본문
  scripts/*.py        실행 코드 (있는 경우)
  contract/*.json     산출물 계약. 구조는 JSON Schema, 그 외(선행조건·멱등성·
                      값 도메인·교차 제약)는 선언적 JSON으로 (있는 경우)
  references/common-mistakes.md  실패 유형과 원인 (있는 경우)
  case/*.json|*.md    실제 사례 파일 (있는 경우)
```

#### SKILL.md 작성방법
- 구성: 폴더 규약의 구체적 정의
- 실행: 작업 진행 방식
- output path: 산출물 경로 규약
